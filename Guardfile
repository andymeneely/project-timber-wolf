group :default do
  guard 'rake', :task => 'all' do
    watch %r{.*\.xlsx$}
    watch %r{.*\.rb$}
    watch %r{.*\.yml}
  end
end

group :characters do
  guard 'rake', :task => 'characters' do
    watch %r{.*\.xlsx$}
    watch %r{.*\.rb$}
    watch %r{.*\.yml}
  end
end
