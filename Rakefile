require 'squib'

task default: [:security, :characters, :skills, :helps]

task :security do
  load 'src/security.rb'
end

task :characters do
  load 'src/characters.rb'
end

task :skills do
  load 'src/skills.rb'
end

task :helps do
  load 'src/helps.rb'
end
